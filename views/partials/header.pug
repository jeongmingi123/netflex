header.header
    .header__column
        a(href=routes.home)
            img(src="../image1.png")
    .header__column
        form(action=routes.search, method="get")
            input(type="text", placeholoder="Search by term", name="term")
    .header__column
        ul
            li
                a(href=routes.join) Join
            li
                a(href=routes.login) Logged In
    .header__column
    .header__column
    .header__column
    .header__column
    .header__column
    .header__column

    